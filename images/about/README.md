# About photo

This folder holds the profile photo used in the site's About section — nothing else.

Currently expected here: **`selfie-kissa.png`** (move it from `images/use for website/`).

Once it's here, `../index.html` references it as:
```html
<img src="images/about/selfie-kissa.png" alt="Kees, de tekenaar achter Line" loading="lazy">
```
